[
  {
    "id": "sample-demo",
    "claim": "Try it with sample data opens a ready handoff card.",
    "where": "landing hero",
    "test": "npm test -- --grep @claim:sample-demo",
    "sandbox": "fresh browser context at /demo"
  },
  {
    "id": "no-account",
    "claim": "No account is needed to open and use a handoff card.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:no-account",
    "sandbox": "fresh browser context at /demo"
  },
  {
    "id": "plain-text-export",
    "claim": "Copies the shopper list as plain text.",
    "where": "handoff card button",
    "test": "npm test -- --grep @claim:plain-text-export",
    "sandbox": "demo list with pasted ingredient lines"
  },
  {
    "id": "local-file-private",
    "claim": "Saves a local handoff file without shopper notes.",
    "where": "handoff card button",
    "test": "npm test -- --grep @claim:local-file-private",
    "sandbox": "demo list with a shopper note"
  },
  {
    "id": "local-file-roundtrip",
    "claim": "A saved local handoff file opens as a shopping list on this device.",
    "where": "handoff card button, README",
    "test": "npm test -- --grep @claim:local-file-roundtrip",
    "sandbox": "save the demo handoff file, then import it into a fresh real list"
  },
  {
    "id": "print-sheet",
    "claim": "Print opens the browser print dialog for the shopping sheet.",
    "where": "handoff card print button, README",
    "test": "npm test -- --grep @claim:print-sheet",
    "sandbox": "demo handoff card with a browser print spy"
  },
  {
    "id": "qr-recipient",
    "claim": "Scanning a QR code opens a readable shopping list in a browser.",
    "where": "QR panel, README",
    "test": "npm test -- --grep @claim:qr-recipient",
    "sandbox": "decode the demo QR, open its URL in a fresh browser context, and verify the recipient list"
  },
  {
    "id": "qr-private",
    "claim": "QR handoff contains item lines, never the shopper note or list title.",
    "where": "privacy section, QR panel",
    "test": "npm test -- --grep @claim:qr-private",
    "sandbox": "demo list with a sensitive-looking title and shopper note"
  },
  {
    "id": "recipient-checkable",
    "claim": "A recipient can check items in a shared list without saving it.",
    "where": "recipient handoff page",
    "test": "npm test -- --grep @claim:recipient-checkable",
    "sandbox": "open a demo QR URL in a fresh browser context and check an item"
  },
  {
    "id": "quantity-normalization",
    "claim": "Combines compatible quantities and keeps uncertain count units visible for review.",
    "where": "ingredient input, handoff warning, README",
    "test": "npm test -- --grep @claim:quantity-normalization",
    "sandbox": "paste compatible metric quantities and repeated count units"
  },
  {
    "id": "local-only",
    "claim": "Stores the demo list in a browser key separate from real lists.",
    "where": "demo banner, privacy page, README",
    "test": "npm test -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo"
  },
  {
    "id": "local-data-private",
    "claim": "Does not send ingredient lists, notes, or device identifiers to a server.",
    "where": "privacy page, README",
    "test": "npm test -- --grep @claim:local-data-private",
    "sandbox": "full populated demo flow while recording every browser request URL and body"
  },
  {
    "id": "offline-reload",
    "claim": "Works offline after the first visit.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:offline-reload",
    "sandbox": "fresh context visits /demo then goes offline and reloads"
  }
]
